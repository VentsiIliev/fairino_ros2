# Requirements for Fairino ROS2 Bridge
# INSTALL WITH pip install -r requirements_bridge.txt

# IF NO VENV:
# python3 -m venv .venv
# source .venv/bin/activate
# INSTALL:  pip install -r requirements_bridge.txt

# Server-side (requires ROS2 environment)
Flask==3.0.0
Flask-CORS==4.0.0  # If you need cross-origin requests
websockets>=12.0

# Client-side (NO ROS2 required!)
requests==2.31.0

# Optional: For ZeroMQ option
# pyzmq==25.1.0

# Optional: For async support
# aiohttp==3.9.0