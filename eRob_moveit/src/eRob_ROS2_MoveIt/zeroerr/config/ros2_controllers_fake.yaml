# ros2_controllers_fake.yaml
# ros2_control configuration for mock_components/GenericSystem

controller_manager:
  ros__parameters:
    update_rate: 200
    use_sim_time: false

    manipulator_controller:
      type: joint_trajectory_controller/JointTrajectoryController

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster


joint_state_broadcaster:
  ros__parameters:
    joints:
      - Joint_1
      - Joint_2
      - Joint_3
      - Joint_4
      - Joint_5
      - Joint_6

    interfaces:
      - position
      - velocity
      - effort

    publish_dynamic_joint_states: true


manipulator_controller:
  ros__parameters:
    joints:
      - Joint_1
      - Joint_2
      - Joint_3
      - Joint_4
      - Joint_5
      - Joint_6

    command_interfaces:
      - position

    state_interfaces:
      - position
      - velocity

    interpolation_method: "splines"

    allow_integration_in_goal_trajectories: true
    allow_nonzero_velocity_at_trajectory_end: true
    allow_partial_joints_goal: false
    open_loop_control: false
    set_last_command_interface_value_as_state_on_activation: false

    state_publish_rate: 100.0
    action_monitor_rate: 200.0

    command_joint_names:
      - Joint_1
      - Joint_2
      - Joint_3
      - Joint_4
      - Joint_5
      - Joint_6

    constraints:
      decelerate_on_cancel: false
      goal_time: 0.0
      stopped_velocity_tolerance: 0.2

      Joint_1: {trajectory: 0.0, goal: 0.1, max_deceleration_on_cancel: 0.5}
      Joint_2: {trajectory: 0.0, goal: 0.1, max_deceleration_on_cancel: 0.5}
      Joint_3: {trajectory: 0.0, goal: 0.1, max_deceleration_on_cancel: 0.5}
      Joint_4: {trajectory: 0.0, goal: 0.1, max_deceleration_on_cancel: 0.5}
      Joint_5: {trajectory: 0.0, goal: 0.1, max_deceleration_on_cancel: 0.5}
      Joint_6: {trajectory: 0.0, goal: 0.1, max_deceleration_on_cancel: 0.5}